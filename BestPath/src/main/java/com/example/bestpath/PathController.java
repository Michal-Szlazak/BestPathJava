package com.example.bestpath;

public class PathController {
/*
    public void setFindButton(Button findButton, Graph currentGraph){
        findButton.setOnAction(new EventHandler<ActionEvent>() {
            @Override
            public void handle(ActionEvent actionEvent) {

                if(VisualGraph.startingVertex != -2 && VisualGraph.endingVertex != -2) {
                    boolean isPathFound = Path.dijkstra(currentGraph.graph, VisualGraph.startingVertex, VisualGraph.endingVertex, currentGraph.rows, currentGraph.columns, Main.pathList);
                    if(isPathFound == true)
                        listPathButtons.addLast(new PathButtons(pathsFoundGridPane, paths, VisualGraph.startingVertex, VisualGraph.endingVertex));
                    currentGraph.vertexes[VisualGraph.startingVertex].setFill(Color.DARKGRAY);
                    currentGraph.vertexes[VisualGraph.endingVertex].setFill(Color.DARKGRAY);
                    VisualGraph.startingVertex = -2;
                    VisualGraph.endingVertex = -2;
                    VisualGraph.chosen = 0;
                }
            }
        });
    }
*/
}
